Need to redirect after meals/new and delete
Need navbar (stateless component)
Welcome page (stateless compoent) with options- create conditional so home option doesnt show
delete button? stateless components
add design to meal and meals components

need to make most recent post render first
on meal show page, its rendering the wrong post-check routes 
need to get rid of dot and quotation
