Need to redirect after meals/new and delete
Need navbar (stateless component)
Welcome page (stateless compoent) with options- create conditional so home option doesnt show
delete button? stateless components
add design to meal and meals components

need text area for caption input

NEED BASIC NAVBAR STYLING
NEED CONTAINER STYLING FOR POSTS 
NEED BASIC FORM STYLING 

   <Link to={`/meals/${meal.id}`}> what is this line



X need to make most recent post render first
X on meal show page, its rendering the wrong post-check routes 
X need to get rid of dot and quotation
